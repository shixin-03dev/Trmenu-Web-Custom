# TrMenu Web Custom - 在线可视化配置系统

## 📖 项目简介 (Introduction)

**TrMenu Web Custom** 是专为 Minecraft 高级菜单插件 [TrMenu V3](https://hhhhhy.gitbook.io/trmenu-v3) 设计的在线可视化配置工具。

TrMenu V3 功能强大，支持动态图标、复杂动作（Actions）、Kether 脚本以及多种交互事件，但其 YAML 配置文件结构较为复杂，手动编写容易出现语法错误或逻辑混淆。本项目旨在提供一个 **"所见即所得" (WYSIWYG)** 的 Web 界面，帮助服务器管理员和开发者快速构建、调试和导出 TrMenu 菜单配置。

## 🎯 核心需求与功能 (Features)

### 1. 可视化布局编辑器 (Visual Layout Editor)
- **网格系统**：模拟 Minecraft 箱子界面（支持 1-6 行及玩家背包区域）。
- **拖拽交互**：支持图标在槽位间的拖拽、复制与交换。
- **实时预览**：在 Web 端尽可能还原游戏内的渲染效果（Lore、名称、材质图标）。

### 2. 智能属性配置 (Smart Property Configurator)
- **基础属性**：材质 (Material)、名称 (Display Name)、描述 (Lore)、自定义模型数据 (CustomModelData)。
- **动态更新**：配置图标的刷新频率 (Update ticks)。
- **条件图标**：可视化编辑 `Priority Icons`（根据条件显示不同图标）。

### 3. 动作与事件构建器 (Action & Event Builder)
- **多交互支持**：分别为左键、右键、Shift+点击、Q键丢弃等 20+ 种交互类型绑定动作。
- **动作向导**：无需记忆 `sound: xxx` 或 `tell: xxx` 语法，通过表单选择动作类型并填写参数。
- **Kether 脚本支持**：提供基础的脚本语法高亮或常用代码片段（Snippets）。

### 4. 导入与导出 (Import & Export)
- **YAML 解析**：支持粘贴现有的 TrMenu YAML 配置并解析到可视化界面。
- **实时生成**：所有的编辑操作实时映射为标准 YAML 格式，一键复制或下载。
- **错误检查**：基础的语法检查，防止 YAML 格式错误。

## 🛠 技术栈 (Tech Stack)

本项目采用现代前端技术栈进行开发：

- **Core**: [React](https://react.dev/) (v18+) - 构建复杂的交互式 UI。
- **Build Tool**: [Vite](https://vitejs.dev/) - 极速开发体验。
- **Language**: [TypeScript](https://www.typescriptlang.org/) - 确保类型安全，特别是复杂的配置结构。
- **Styling**: [Tailwind CSS](https://tailwindcss.com/) - 快速构建美观的界面。
- **State Management**: Zustand 或 Redux Toolkit - 管理庞大的菜单状态树。
- **Parsers**: `js-yaml` - 处理 YAML 的读写。
- **UI Components**: [Shadcn/ui](https://ui.shadcn.com/) 或 Ant Design - 高质量的基础组件库。

## 🚀 快速开始 (Getting Started)

### 环境要求
- Node.js >= 18.0.0
- npm / yarn / pnpm

### 安装依赖
```bash
npm install
# 或者
pnpm install
```

### 启动开发服务器
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

## 📂 目录结构规划 (Structure)

```text
src/
├── assets/          # 静态资源（如 Minecraft 材质贴图）
├── components/      # UI 组件
│   ├── editor/      # 编辑器核心组件 (Grid, Sidebar)
│   ├── preview/     # 预览组件
│   └── ui/          # 通用基础组件
├── hooks/           # 自定义 Hooks
├── lib/             # 工具库 (YAML 解析, TrMenu 类型定义)
├── store/           # 状态管理
└── styles/          # 全局样式
```

## 🔗 参考资料
- [TrMenu V3 官方文档](https://hhhhhy.gitbook.io/trmenu-v3)
- [TrMenu GitHub 仓库](https://github.com/TrPlugins/TrMenu)
